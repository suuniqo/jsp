read var;

let int a = var + ((23));

function int noreturn(void) {}

function void noop(void) {return;return;return;}

write implicit;

implicit = var + - + - implicit;

function int other_scope(float var) {
    var = 5.0;
    var = 6.0;
}

let string mystr = "mystr";

function int last_try(void) {
    implicit = 0;

    let float implicit;

    implicit = 0.0;
}
