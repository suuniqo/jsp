asdf &= true;

extra = "hi";
let float asdf = 3 + first;

first = "hello";
read sec;

sec = 5.0;

let boolean hey = true && third;

let boolean a = false;

function int oper(int a, float b, string c) {
    let int hey = 3;
    hey = hey + 4;

    a = 5;
    let int a = 5;

    yo = oper(1, 2, 3, 4);
    read troll;

    let float oper = 3.0;

    oper(1, 1.0, "1");

    oper = 2.0;

    return oper(c, hey, a);
}

let float lol_sum = oper();
let string troll = "3o4";

a = true;

function float call1(void) {
    return "hola";
    return !!(1 < random) && (1 == 1);
    return 3 + oper(3 * (-7 + oper(6,6.0,"6")), lol_sum, ".");
}

function void call2(void) {
    return;
    return false;
    return;
    return 1 == blop;
}

function void noop(void) { return; return; return; }

doom = noop(pram1, pram2, pram3);

function int repeat(int x, float x) {
    x = 5.0;
    return repeat(x);
}
